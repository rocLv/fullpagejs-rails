require 'spec_helper'

describe Fullpagejs::Rails do
  it 'has a version number' do
    expect(Fullpagejs::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
